
get:
  tags:
    - chat-messages
  summary: 一覧
  description: description
  operationId: chat-messages-list
  security:
    - Authentication: []
  parameters:
    - $ref: "../requests/parameters/path.yml#chat_room_uuid"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../responses/resources/models/chat-message.yml"
post:
  tags:
    - chat-messages
  summary: 作成
  description: description
  operationId: chat-messages-create
  security:
    - Authentication: []
  parameters:
    - $ref: "../requests/parameters/path.yml#chat_room_uuid"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/resources/chat-messages-create.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/resources/models/chat-message.yml"
