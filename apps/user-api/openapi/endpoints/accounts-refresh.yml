
post:
  tags:
    - accounts
  summary: リフレッシュトークンから新しいトークンを取得する
  description: description
  operationId: accounts-refresh
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/resources/accounts-refresh.yml"
  responses:
    "200":
      description: 成功した場合
      content:
        application/json:
          schema:
            $ref: "../responses/resources/accounts-token.yml"
