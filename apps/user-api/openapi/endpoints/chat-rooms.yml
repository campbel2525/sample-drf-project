
get:
  tags:
    - chat-rooms
  summary: 一覧
  description: description
  operationId: chat-rooms-list
  security:
    - Authentication: []
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../responses/resources/models/chat-room.yml"
post:
  tags:
    - chat-rooms
  summary: 作成
  description: description
  operationId: chat-rooms-create
  security:
    - Authentication: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/resources/chat-rooms-create.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/resources/models/chat-room.yml"
