
post:
  tags:
    - accounts
  summary: 新規登録
  description: description
  operationId: accounts-signup
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/resources/accounts-signup.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/resources/accounts-token.yml"
