get:
  tags:
    - accounts
  summary: 自分の情報を取得する
  description: description
  operationId: accounts-me
  security:
    - Authentication: []
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/resources/models/admin-user.yml"
