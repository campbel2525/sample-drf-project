
post:
  tags:
    - accounts
  summary: ログイン
  description: description
  operationId: accounts-login
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/resources/accounts-login.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/resources/accounts-token.yml"
